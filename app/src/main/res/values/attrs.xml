<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="ScaledLayout">
        <attr name="layout_width" format="integer" />
        <attr name="layout_width_measure" format="enum">
            <enum name="fixed" value="0"/>
            <enum name="relative" value="1"/>
        </attr>
        <attr name="layout_height" format="integer" />
        <attr name="layout_height_measure" format="enum">
            <enum name="fixed" value="0"/>
            <enum name="relative" value="1"/>
        </attr>
        <attr name="offset_layout_left" format="integer" />
        <attr name="offset_layout_top" format="integer" />
        <attr name="offset_layout_top_invert" format="boolean" />
        <attr name="relative" format="boolean" />
        <attr name="layout_position" format="enum">
            <enum name="block" value="0"/>
            <enum name="relative" value="1"/>
            <enum name="absolute" value="2"/>
        </attr>
    </declare-styleable>

    <declare-styleable name="ScaledView">
        <attr name="offset_left" format="integer" />
        <attr name="offset_top" format="integer" />
        <attr name="scaled_width" format="integer"/>
        <attr name="scaled_height" format="integer" />
        <attr name="rotation" format="float" />
    </declare-styleable>

    <declare-styleable name="ImageView">
        <attr name="src" format="reference" />
    </declare-styleable>

    <declare-styleable name="TextView">
        <attr name="text_size" format="integer" />
        <attr name="text_color" format="color" />
        <attr name="align" format="enum">
           <enum name="right" value="-1"/>
           <enum name="center" value="0"/>
           <enum name="left" value="1"/>
        </attr>
        <attr name="box_width" format="integer" />
    </declare-styleable>

</resources>
